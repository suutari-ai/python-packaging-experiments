Experiments with Python packaging
=================================

This is a playground for experimenting with Python packaging, i.e. some
setup.py/setup.cfg stuff.

Create a virtualenv and run ``test.sh`` to try it out.
